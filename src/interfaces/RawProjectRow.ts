export default interface RawProjectRow {
  title: string;
  date: string;
  tags: string;
  category: string;
  description: string;
  hide: boolean;
  link: string;
  // [key: string]: string;
}
